#!/usr/bin/env direnv
# Auto-activate Python venv when entering this directory

if [ -d ".venv" ]; then
    source .venv/bin/activate
    echo "✅ MizzouNewsCrawler venv activated"
else
    echo "⚠️  .venv directory not found. Run: python3.11 -m venv .venv"
fi
