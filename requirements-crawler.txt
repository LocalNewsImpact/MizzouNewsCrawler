# Crawler-specific requirements
# These packages are only needed for the crawler service
# that handles discovery, extraction, and verification

# FastAPI for work queue service
fastapi>=0.141.1
uvicorn[standard]>=0.52.4

# Web scraping and browser automation
selenium>=4.48.0
undetected-chromedriver>=3.5.5  # Advanced anti-detection
selenium-stealth>=1.0.6  # Browser fingerprint masking
brotli>=1.2.0  # Support Brotli compression for HTTP responses

# Article extraction
newspaper4k>=0.9.6  # Use newspaper4k (modern fork)
# mcmetadata package vendored in src/mcmetadata
dateparser>=1.4.2
htmldate>=1.10.0
trafilatura>=2.2.0
boilerpy3>=1.0.7
goose3>=3.1.22
readability-lxml>=0.9
py3langid>=0.4.0
tldextract>=5.3.2
url-normalize>=3.0.0
furl>=2.1.4
feedparser>=6.0.14

# MediaCloud integration for headline duplication checks
mediacloud>=5.1.0

# Article classification
scikit-learn>=1.9.0,<1.10  # Compatible with StorySniffer 1.0.9+ skops models (model trained with 1.7.2)
storysniffer>=1.0.9  # News article classification (skops-based models)

# Anti-bot bypass
cloudscraper>=1.2.71  # Bypass Cloudflare protection
tls-client>=1.0.1  # Optional: emulate Chrome TLS/HTTP2 fingerprints when available

# Secret management for secure credential retrieval
google-cloud-secret-manager>=2.30.0
