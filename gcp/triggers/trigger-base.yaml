# Manual trigger for base image build
# Use: gcloud builds triggers run build-base-manual

name: build-base-manual
description: Manually build shared base image with common dependencies
filename: gcp/cloudbuild/cloudbuild-base.yaml
