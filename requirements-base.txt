# Base requirements - Common dependencies used by all services
# This file contains ~80% of dependencies shared across API, Processor, and Crawler

# Core data processing
pandas>=3.0.3
numpy>=2.4.6  # Python 3.11+ supports numpy 2.x with all dependencies
pyarrow>=25.0.0  # For Parquet support
openpyxl>=3.1.5  # Excel export support

# Database
sqlalchemy>=2.0.51
alembic>=1.18.5
psycopg2-binary>=2.9.12  # PostgreSQL adapter for production
cloud-sql-python-connector[pg8000]>=1.20.4  # Google Cloud SQL connector

# HTTP and web utilities
requests>=2.34.2
urllib3>=2.7.0
beautifulsoup4>=4.15.0
lxml>=6.1.1
lxml_html_clean>=0.4.5  # Required for newspaper3k HTML cleaning
storysniffer>=1.0.9  # URL verification service (skops-based models)
selenium>=4.46.0  # Needed by tests
newspaper4k>=0.9.6  # Needed by tests
feedparser>=6.0.12  # Needed by tests
rapidfuzz>=3.14.5  # Needed by tests
cloudscraper>=1.2.71  # Needed by tests

# NLP and text processing
spacy>=3.8.14  # Python 3.11+ supports latest spacy with numpy 2.x
nltk>=3.10.0
textblob>=0.20.1
ftfy>=6.3.1
python-dateutil>=2.9.0.post0
scikit-learn>=1.9.0,<1.10  # Compatible with StorySniffer 1.0.9+ skops models (model trained with 1.7.2)

# Testing
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-mock
requests-mock
fastapi>=0.139.2  # Needed for test fixtures
httpx  # Needed for FastAPI TestClient

# Development
pre-commit>=4.6.0
isort>=8.0.1
flake8>=7.3.0

# Utilities
click>=8.4.2
python-dotenv>=1.2.2
pydantic>=2.13.4
pyyaml>=6.0.3  # For Kubernetes manifest generation

# Observability and Monitoring
structlog>=26.1.0  # Structured logging
google-cloud-logging>=3.16.1  # Cloud Logging integration
google-cloud-monitoring>=2.31.0  # Cloud Monitoring metrics
google-cloud-storage>=3.13.0  # Raw HTML archive (extractor A/B replay)
# Needed by api, crawler and processor, so it belongs here rather than being
# repeated in each service's requirements.
google-cloud-secret-manager>=2.30.0  # Secret retrieval
