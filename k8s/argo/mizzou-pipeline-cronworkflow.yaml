apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: mizzou-news-pipeline
  namespace: production
  labels:
    dataset: mizzou-missouri-state
    type: pipeline
spec:
  schedule: "0 */6 * * *"  # Every 6 hours (00:00, 06:00, 12:00, 18:00 UTC)
  timezone: "UTC"
  concurrencyPolicy: "Forbid"  # Don't run concurrent pipelines
  successfulJobsHistoryLimit: 5
  failedJobsHistoryLimit: 5
  workflowSpec:
    entrypoint: mizzou-pipeline-wrapper
    serviceAccountName: argo-workflow
    
    templates:
    # Wrapper that calls the reusable template
    - name: mizzou-pipeline-wrapper
      steps:
      - - name: run-pipeline
          templateRef:
            name: news-pipeline-template
            template: pipeline
          arguments:
            parameters:
            # Dataset configuration
            - name: dataset
              value: "Mizzou Missouri State"
            # Discovery parameters
            # No source-limit = process ALL 157 sources in dataset each run
            - name: max-articles
              value: "50"
            - name: days-back
              value: "7"
            # Verification parameters
            - name: verify-batch-size
              value: "10"
            - name: verify-max-batches
              value: "100"
            # Extraction parameters
            # extract-limit: articles per batch (balance: larger = faster, smaller = safer rate limiting)
            # extract-batches: max batches (extract-limit × batches = total capacity per run)
            # Current capacity: 50 × 40 = 2,000 articles per run (sufficient for weekly publications)
            - name: extract-limit
              value: "50"
            - name: extract-batches
              value: "40"
            # Moderate rate limiting for Mizzou
            - name: inter-request-min
              value: "5.0"
            - name: inter-request-max
              value: "15.0"
            - name: batch-sleep
              value: "30.0"
            - name: captcha-backoff-base
              value: "1800"    # 30 minutes
            - name: captcha-backoff-max
              value: "7200"    # 2 hours
